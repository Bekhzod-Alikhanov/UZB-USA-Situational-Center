overrides:
  "postcss@<8.5.10": ">=8.5.10"

# Native deps allowed to run install/build scripts. Without this, pnpm 10 on
# CI (corepack) fails the install with ERR_PNPM_IGNORED_BUILDS instead of
# silently skipping. Generated by approving the build scripts pnpm flagged.
onlyBuiltDependencies:
  - "@parcel/watcher"
  - "@swc/core"
  - sharp
  - unrs-resolver
